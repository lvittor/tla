# Intentar asignar una variable de un tipo basico a una distribucion de probabilidad.
main {

binomial(10, 0.35, 3) dist1;

# Esto se rompe ya que basic_type no devuelve lo mismo que dist1
# que seria un float -> si basic_type fuera float, si funcionaria
integer basic_type = dist1;

}

